<Window x:Class="GYM_CLIENT.View.Admin.Forms.PaymentForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GYM_CLIENT.View.Admin.Forms" xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d"
        Title="PaymentForm" 
        Height="540" 
        Width="300"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Loaded="Window_Loaded"
        >

    <Border Background="#F4F5F6"
            BorderBrush="Gray"
            BorderThickness="0.5"
            CornerRadius="10"
            >

        <Grid>

            <Grid.RowDefinitions>

                <RowDefinition Height="40"/>
                <RowDefinition/>
                <RowDefinition Height="340"/>
                <RowDefinition Height="50"/>

            </Grid.RowDefinitions>
            
            <!--Header-->
            <Border Grid.Row="0"
                    CornerRadius="9,9,0,0"
                    Background="#313638"
                    >

                <Grid>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="50"/>
                    </Grid.ColumnDefinitions>


                    <!--Close button-->
                    <Button x:Name="CloseBtn"
                                       Style="{StaticResource MinimizeButtonStyle}"
                                       Grid.Column="2"
                                       HorizontalAlignment="Right"
                                       Margin="0,0,4,0"
                                       Cursor="Hand"
                                       Click="CloseBtn_Click">


                        <Icon:PackIconMaterial 
                                            Kind="Close" 
                                            VerticalAlignment="Center"
                                            Height="10"
                                            HorizontalAlignment="Right"
                                            Width="16"
                                        />

                    </Button>
                </Grid>
                
            </Border>
            
            <!--Scanner-->
            <Border Grid.Row="1">

                <Grid>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="190"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    
                    <!--Camera Border-->
                    <Border BorderBrush="Gray"
                            Background="White"
                            BorderThickness="0.5"
                            Margin="3"
                            Grid.Column="1"
                             CornerRadius="10" >

                        <Image x:Name="CameraImage" 
                               Stretch="Fill"/>


                    </Border>
                </Grid>
                
            </Border>
            
            <!--Content-->
            <Border Grid.Row="2"
                    Background="White"
                    Margin="10"
                    CornerRadius="5"
                    >
                <StackPanel Orientation="Vertical">
                     <!--Client name-->
                     <Label Content="Client(ReadOnly)"/>
                    <TextBox x:Name="ClientName" IsReadOnly="True" Style="{StaticResource TextBoxes}"
                             MaxLength="30"
                             />

                      <!--PaymentType-->
                      <Label Content="PaymentType"/>
                    <ComboBox x:Name="PaymentType" Height="30" SelectedValuePath="Content">
                        <ComboBoxItem Content="Cash"/>
                        <ComboBoxItem Content="Gcash"/>
                    </ComboBox>


                    <!--PlanName-->
                    <Label Content="Membership Price"/>
                    <TextBox x:Name="MembershipPrice" IsReadOnly="True" Style="{StaticResource TextBoxes}"
                      MaxLength="6"
                      />


                    <!--Client name-->
                    <Label Content="Amount"/>
                    <TextBox x:Name="Amount" Style="{StaticResource TextBoxes}"
                         MaxLength="4"
                          PreviewKeyDown="Amount_PreviewKeyDown"
                          TextChanged="Amount_TextChanged_1"
                             PreviewTextInput="Amount_PreviewTextInput"
                         />

                    <!--Change-->
                    <Label Content="Change(ReadOnly)"/>
                    <TextBox x:Name="Change" IsReadOnly="True"
                      Style="{StaticResource TextBoxes}"
                      MaxLength="6"
                      />


                    <!--Change-->
                    <Label Content="Days left for Membership(Readonly)"/>
                    <TextBox x:Name="DaysleftTxt" IsReadOnly="True"
                      Style="{StaticResource TextBoxes}"
                             TextChanged="DaysleftTxt_TextChanged"
                      MaxLength="3"
                      />

                </StackPanel>
              
           

                
            </Border>
            
            <!--Footer-->
            <Border Grid.Row="3"
                    CornerRadius="0,0,9,9"
                    BorderBrush="Gray"
                    Background="White"
                    BorderThickness="0.5"
                    >
                <Button x:Name="Submit" Grid.Column="1" 
                      Style="{StaticResource SubmitButton}" 
                      Content="Pay"
                      Cursor="Hand"
                      Background="Green"
                      Click="Submit_Click"
                      BorderThickness="0" 
                      HorizontalAlignment="Center" Margin="0,12,0,12" Width="278" 
                  />

            </Border>
        </Grid>
        
    </Border>
    
</Window>
